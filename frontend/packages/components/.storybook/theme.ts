import { create } from "@storybook/theming";

export default create({
  base: "light",
  brandTitle: "HasteHealth Components",
  brandUrl: "https://haste.health",
  brandImage: "https://haste.health/img/logo.svg",
  brandTarget: "_self",
});

import { create } from "@storybook/theming";

export default create({
  base: "light",
  brandTitle: "HasteHealth Components",
  brandUrl: "https://haste-health.app",
  brandImage: "https://haste-health.app/img/logo.svg",
  brandTarget: "_self",
});

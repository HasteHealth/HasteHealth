{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "(OSX) Launch",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceRoot}/target/debug/server",
      "args": [],
      "env": {
        "DATABASE_URL": "postgres://postgres:postgres@localhost/haste_health"
      },
      "cwd": "${workspaceRoot}"
    }
  ]
}
